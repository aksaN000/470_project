# MemeStack Backend Environment Variables - Production Template
# Copy this file to .env and update with your actual production values

# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration
# MongoDB Atlas (Required for production)
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/memestack?retryWrites=true&w=majority

# JWT Secret Key (REQUIRED: Generate a strong random secret for production!)
JWT_SECRET=your-super-secret-jwt-key-here-min-32-characters

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# CORS Configuration (Frontend URL)
CLIENT_URL=https://your-domain.com

# Cloudinary Configuration (Optional - for image uploads)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Security
BCRYPT_ROUNDS=12
